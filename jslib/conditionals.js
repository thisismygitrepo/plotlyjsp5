

b = 'haha';

switch (b.toLowerCase()){

    case 'new':
        console.log(22);
        break;
    default:
        console.log(44);

}

a = 2 === 3 ? 2 : 4;  // strict inequality doesn't allow type coersion.
console.log(a)

